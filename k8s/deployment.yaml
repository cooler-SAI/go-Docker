apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-docker-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-docker
  template:
    metadata:
      labels:
        app: go-docker
    spec:
      containers:
        - name: go-docker-container
          image: go-docker
          ports:
            - containerPort: 8080
